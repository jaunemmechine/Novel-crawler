<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>小说-<%=category%></title>
    <style>
        .div{
            transform-style:preserve-3d;
            width:120px;
            height: 150px;
            position: relative;
            transition:all 1s;
            margin: 5px;

        }
        .div:hover{
            transform:rotateY(180deg);
        }
        .div .div1 img{
            position: absolute;top: 0px;left: 0px;
            width: 120px;height: 150px;
        }
        .div .div2 img{
            z-index: 2;
            position: absolute;top: 0px;left: 0px;
            width: 120px;height: 150px;
            transform:rotateY(180deg);
        }

    </style>
<body>
<% include head.ejs%>
<div class="text-center">
    <div class="btn-group">
        <%if(page>1){%>
        <a href="/?category=<%=category%>&searchTxt=<%=searchTxt%>&page=<%=page-1%>" class="btn btn-primary" >
            <span class="glyphicon glyphicon-search"></span>
            上一页
        </a>
        <%}%>
        <%if(isNextPage){%>
        <a href="/?category=<%=category%>&searchTxt=<%=searchTxt%>&page=<%=page+1%>" class="btn btn-primary">
            <span class="glyphicon glyphicon-search"></span>
            下一页
        </a>
        <%}%>
    </div>
</div>
<div style="overflow:hidden;zoom:1;">
    <%for(var item of rows){%>
    <div class="col-xs-6 col-sm-4 col-md-3 col-lg-2">
        <div class="div">
            <div class="div1"><img src="<%=item.imgUrl%>"></div>
            <div class="div2"><img src="<%=item.imgUrl%>"></div>
        </div>
        <div style="width:220px;color: #0000ff"><%=item.name%></div>
        <div style="width:150px;height:30px;font-size: 12px;">作者：<%=item.author%></div>
        <div  style="height:100px;overflow:hidden;margin-top: 10px"><%=item.description%></div>
        <p class="btn-group" style="margin-top: 5px">
            <a href="/viewnovel?id=<%=item._id%>" class="btn btn-primary" role="button">
                查看章节
            </a>
            <a href="/viewsection?id=<%=item._id%>" class="btn btn-default" role="button">
                阅读
            </a>
        </p>
    </div>
    <%}%>
</div>
<% include footer.ejs%>
</body>
</html>